/**
 * Shared motion tokens.
 *
 * EASE is the site's standard entrance/reveal easing curve. It was previously
 * copy-pasted as a local `const EASE` in every animated section; keep it here so
 * a motion-feel change is a single edit instead of N drifting copies.
 */
export const EASE = [0.32, 0.72, 0, 1] as const;
