# Noon Redesign — Reference Atlas

Persistent visual breakdown of the owner's reference sites — **the quality / atmosphere / sophistication bar** for the redesign. Owner (2026-06-23, emphatic): *"same vibes, same sophistication, own identity — do NOT copy literally, and do NOT miss a single one."*

Each entry = what it is · its layers · **what Noon adopts** (translated into our locked system, never pasted). System = [specs/2026-06-23-redesign-design-tokens.md](../../specs/2026-06-23-redesign-design-tokens.md).

> Images live in chat (not saved to disk). This atlas is the durable proxy — written in enough detail to build against without re-viewing.

---

## Batch 1 — received 2026-06-23 (4 sites)

### EchelonAI — "Knowledge & compliance for humans and agents"
- **Architecture:** hero (subtle iridescent mesh bg + product shot) → logo strip → "Centralized & Unified performance" → 3-col feature rows w/ icons → "Compliance built for AI agents" section → FAQ → CTA card → **dark footer with OVERSIZED "EchelonAI" wordmark.**
- **Type:** sans grotesk display, medium weight, tight tracking; mono micro-labels.
- **Color:** light/white dominant, near-black text; ONE subtle iridescent gradient in the hero (blue→pink mesh) = the only non-flat moment; dark footer.
- **Grid / ticks:** STRONG technical grid — 1px hairlines + **corner crosshairs (+) at intersections**, present across the feature/compliance sections (not just frame corners).
- **Components:** "Workflow Automation" card = code snippet + **97% donut metric**; 3-col Validation / Vulnerability / Visibility each with a mini-illustration; **concentric-ripple radar data-viz** (Transparency / Audit report / Agents log).
- **Atmosphere:** clinical, trustworthy, airy, technical-premium.
- **→ Noon adopts:** the page architecture; the crosshair grid in technical sections; code+metric card; concentric/abstract data-viz; oversized footer wordmark. **Skip** the iridescent gradient (we're flat/mono).

### Paradigm — "The spreadsheet to automate manual data collection"
- **Architecture:** DARK editorial hero (serif display + 2 CTAs) → product screenshot overlapping → "Trusted by industry leaders" logo strip → LIGHT "A workspace to transform your data" → **Step 1 / 2 / 3 stepped progress (Step 2 active = blue)** → 3-col Upload / Enrich / Take Action w/ mini-mockups.
- **Type:** **EDITORIAL SERIF display** (big, refined) — its signature. Sans for body/UI.
- **Color:** black hero + white sections; blue accent (active step, the "Uploaded" check); mostly mono.
- **Grid / ticks:** hairline column grid + corner ticks; sections framed by thin rules.
- **Components:** stepped progress (numbered, blue active); 3-col with small UI mockups; data-table product shot; "Try it now" (solid) / "Request a Demo →" (outline) pill CTAs.
- **Atmosphere:** editorial, confident, data-precise, premium.
- **→ Noon adopts:** stepped progress (blue active); the dark-editorial-hero option; 3-col with mini-mockups; hairline-framed sections. **SERIF = open decision (see Tensions).**

### SDK / MCP platform — "Build better products. SDKs for your users, MCP servers for LLMs."
- **Architecture:** centered SERIF display intro + tiny stacked-layers icon w/ a thin line + micro rainbow accent → **"API TOOLCHAIN" boxed mono kicker** + "From design to distribution" (serif) + description → 3-col **NUMBERED 01 / 02 / 03** feature row (Design / Distribute / Develop) each with a small technical mockup/diagram.
- **Type:** editorial serif display; mono kicker inside a bordered box.
- **Color:** white/airy; mono; one tiny rainbow gradient accent at the icon (otherwise flat).
- **Grid / ticks:** prominent hairline grid dividing the 3 columns + the section; corner ticks at intersections.
- **Components:** boxed mono kicker; numbered 01/02/03 columns; mini diagrams (code preview, node "→ SUCCESS", "USE YOUR APIS WITH …" pill row).
- **Atmosphere:** developer-premium, precise, calm, lots of air.
- **→ Noon adopts:** the boxed mono kicker; numbered 01/02/03 feature rows with small technical diagrams; the strong hairline column grid; generous whitespace. **SERIF = open decision.**

### CeSIA — "Preparing the world for the challenges of AI"
- **Architecture:** light hero (sans display + 2 CTAs) with a **DOT-FIELD wave illustration on the right** → logo strip ("OUR CURRENT PARTNERS") → large statement line with **muted-word emphasis** ("research on AI safety" rendered lighter) → subtle dotted bg texture.
- **Type:** clean SANS grotesk display; mono micro-labels. (Closest to our locked Geist direction.)
- **Color:** white/mono, near-black; very sparse blue; subtle grey dots.
- **Illustration:** THE abstract-systemic signature — a flowing **dot-field / particle wave** (monochrome, hint of blue). Exactly our "abstract systemic" default.
- **Components:** dark "Get started" + outline "Join Us" pills; logo strip; muted-word emphasis in big headings.
- **Atmosphere:** institutional, clean, serious, airy.
- **→ Noon adopts:** the dot-field illustration (our signature); sans editorial hero; muted-word emphasis in large headings; the sparse mono palette.

---

## Cross-cutting confirmations (these refs VALIDATE the locked spine)
- **Hybrid grid + corner crosshairs** → all four use it; lean into it harder in technical sections (ticks at intersections, not just frame corners).
- **Numbered 01/02/03 feature rows with small technical diagrams** → SDK, EchelonAI.
- **Stepped progress, blue active** → Paradigm. **Mono kicker (often boxed)** → SDK, CeSIA, EchelonAI.
- **Abstract-systemic illustration** (dot-field, concentric radar) → CeSIA, EchelonAI.
- **Airy editorial whitespace + sparse blue + flat** → all four.

---

## Batch 2 — received 2026-06-23 (5 sites)

### "Design that delivers" (agency)
- **Architecture:** light; big sans display SPLIT hero (headline left, sub-copy + **"Our Services" pill + black circle ●-icon-button** right) → "/WORK" mono kicker + "MENU" pill (w/ dot) → **4-col NUMBERED 001–004** feature row.
- **Type:** large sans grotesk display, tight.
- **Components:** the **PILL + BLACK CIRCLE ICON-BUTTON** signature; "MENU" pill w/ dot; mono "/WORK" kicker; numbered 001/002/003/004 cards.
- **Illustration:** thin-line monochrome abstract per card (connected-node path, overlapping circles/Venn, isometric arrow, node cluster).
- **→ Noon adopts:** pill + circle-icon-button; numbered feature cards w/ thin-line abstract illustrations; split editorial hero; mono "/section" kicker.

### rekord — "Run critical decisions with confidence."
- **Architecture:** announcement bar → **pill-grouped nav** → hero (sans display left + **WIREFRAME GLOBE** right) → technical section with BIG corner crosshairs (+) → muted list with one active item (left-border accent) + isometric tile-field illustration.
- **Type:** sans grotesk display.
- **Color:** white/mono, sparse blue (globe nodes, the iso logo).
- **Grid / ticks:** the STRONGEST of all — large (+) crosshairs at section corners; active list item gets a left-border + dark text, rest muted.
- **Illustration:** WIREFRAME GLOBE (lat-long sphere + blue nodes) = our wireframe signature; isometric diamond-tile field.
- **→ Noon adopts:** pill-grouped nav (option); the prominent (+) crosshairs; the wireframe globe; muted-list-with-active emphasis.

### Acme AI
- **Architecture:** stacked feature sections (White-Box Automation, Self-Discovery), each = intricate isometric line-art + title + copy, hairline-divided.
- **Illustration:** CRAFTED ISOMETRIC line-art — book/ledger (open + closed), floating wireframe UI panels (V2.0, Documentation), ruler, gear/aperture, signal arcs, dotted measurement grids. **Blueprint quality — THE high bar for isometric "how it works."**
- **→ Noon adopts:** the isometric CRAFT LEVEL for any process/"how it works" art (far beyond a few boxes); blueprint/measurement-grid texture, wireframe devices.

### Timeglass — "Work normally. Timeglass does the rest."
- **Architecture:** clean white card on grey → centered sans display → **Step 1–4 stepped progress** (Step 1 active = black pill) → 4-col each with title + copy + **small isometric illustration**.
- **Components:** 4-step stepped progress (black active); per-step isometric mini-illustrations; corner ticks on the card.
- **→ Noon adopts:** stepped progress (black active, up to 4 steps); per-step isometric mini-illustrations; the clean carded process block.

### Pathly — "Every link becomes leverage"
- **Architecture:** **FRAMED page** (bordered) → nav (logo + dropdowns + Log in + "Start for free" pill) → hero (**badge-pill** + sans display + 2 CTAs + **node-graph network** illustration) → "Trusted by" logo strip → **two-column text + product-UI** sections with "Learn more ›".
- **Components:** badge-pill ("Now in X · Read more →"); node-graph network illustration; logo strip (real); two-col text+UI; "Learn more ›" links; framed page layout.
- **→ Noon adopts:** the framed layout (our site already has one — refine, don't discard); node-graph illustration; the badge-pill; two-col text+UI rows (this is exactly our case-study row pattern).

---

## Tensions — RESOLVED (all 10 in)
1. **Serif:** of the 8 display-bearing refs, **6 are SANS** (CeSIA, EchelonAI, agency, rekord, Timeglass, Pathly) vs **2 serif** (Paradigm, SDK). **Geist (sans) is the majority bar → confirmed.** Serif = optional minority flavor; can A/B on the hero if curious, not needed.
2. **Gradients:** stay **flat/mono** — only EchelonAI/SDK had subtle ones; the other 7 are flat.

## Trust without client logos
Most refs lean on "Trusted by" logo strips (Pathly: Anthropic/ClickUp; Paradigm: Cognition/etched; CeSIA: universities). Noon has **no real client logos → OMIT that strip**; trust comes from the REAL case studies + metrics + the human-review gate. Optional **honest "built with"** strip only (Next.js/Stripe/Vercel/Anthropic — real tech).

## The illustration system, grounded in the refs
- **Abstract systemic (default):** wireframe globe (rekord), node-graph network (Pathly), node cluster / Venn / path (agency), concentric radar (EchelonAI), dot-field (CeSIA).
- **Isometric (how it works only):** crafted blueprint scenes (Acme), per-step device minis (Timeglass).
- All: thin line, monochrome, sparse blue, same weight → one family.

---

## Vercel — the underlying aesthetic (NORTH STAR)

The owner's whole reference set descends from the **"Vercel aesthetic."** Owner's original captures were lost (Cowork) and archive.org is blocked from fetch, so this is reconstructed from **Vercel's own Web Interface Guidelines** + the "Blueprint Grid" analysis. Key fact: **Vercel created Geist** (our locked font) → **Noon's locked system IS the Vercel system.**

- **Type:** clean monospaced-influenced sans — **Geist Sans (body) + Geist Mono (code/labels)**. Title Case for headings/buttons (Vercel convention — open choice for us). **Tabular numbers** for data/metrics; curly quotes, real ellipsis "…", non-breaking spaces for units ("20 h/week").
- **Grid:** subtle lines/dots behind content — **10–20% opacity max, 1px, light-gray (~#E5E7EB), 16/24px (or 8px) spacing**. linear-gradient = lines, radial-gradient = dots. (The corner crosshairs/ticks we use are layered ON TOP by the *derived* refs — rekord/EchelonAI — the base is the plain grid.)
- **Color:** black + white primary, **2–3 colors max**, minimal accent → our mono-forward, confirmed.
- **Spacing:** generous whitespace; systematic; everything aligned deliberately (grid/baseline/edge/optical center); optical alignment (±1px).
- **Motion:** animate only when it "clarifies cause & effect or adds deliberate delight"; GPU props (transform/opacity); honor prefers-reduced-motion; interruptible → **confirms our intentional-motion decision verbatim.**
- **Components:** visible focus rings, hit targets ≥24px (44 mobile), optimistic updates, "…" loading states ("Generating…").
- **Signature = "invisible excellence":** precision, accessibility, performance, copy clarity — not decoration.
- **Homepage IA — CAPTURED 2026-04-23 from Wayback → `design/references/vercel-old/` (8 shots):** announcement bar → nav (logo + Products/Resources/Solutions dropdowns + Ask AI / Log In / Sign Up) → **CENTERED hero** ("Build and deploy on the AI Cloud" + 2 pill CTAs + the Vercel triangle on a single rainbow-gradient glow) → customer-proof stat band (runway / Leonardo.AI / Zapier + metrics) → product-capability grid ("Your product, delivered" — Agents / AI Apps / Web Apps / Composable Commerce / Multi-tenant) → "From code to infrastructure in one git push" **node-graph** (framework logos → triangle → resource icons, sparse colored connectors) → inline-pill statement ("Scale your `[Enterprise]` without compromising `[Security]`") → "Deploy once, deliver everywhere" (global) → dark compute / Active-CPU-pricing section → developer-platform code block → "Deploy your first app in seconds" framework grid → "Start Deploying" CTA → **massive organized footer** (10+ mono-UPPERCASE column groups + "ALL SYSTEMS NORMAL" status dot + theme toggle).
- **Captured LOOK (confirms + adds):** light/white dominant, Geist throughout; mono-forward **except ONE colorful gradient moment** (the hero triangle glow) — everything else flat. **Corner crosshairs (+) ARE used** at grid intersections (straight from Vercel → our ticks confirmed, despite the article saying otherwise). Black pill primary + outline secondary. Mono-uppercase kickers with small icons. Node-graph illustrations with **sparse colored connectors**. Inline-pill emphasis inside statement lines.

**→ Noon adopts / sharpens:** tabular numbers · grid specs (opacity/weight/spacing) + **corner crosshairs (confirmed)** · ellipsis + non-breaking-space polish · optical alignment · visible focus rings · the **massive organized footer** pattern (grouped mono-header columns + a system-status line) · **inline-pill emphasis** · **(DECISION) the one hero gradient moment** — Vercel (our north star) uses exactly one, in the hero; owner chose flat/mono → worth reconsidering for Noon's hero only. (Title Case headings still optional.)
